/* this test checks tuple element access expressions */

procedure p() returns tuple (integer, integer) {
    return (1, 2);
}

procedure main() returns integer {
    var tuple (integer, integer) t = (10, 20);
    var integer a = t.2;
    var integer b = t.1;
    tuple (integer, integer) u = p();
    a = u.1;
    b = a.1;

    return 0;

}

//CHECK_FILE:1.out
