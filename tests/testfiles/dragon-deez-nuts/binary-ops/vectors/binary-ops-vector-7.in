typealias boolean bool;
procedure main() returns integer {
    vector<integer> a1 = [1];
    vector<integer> a2 = [3];
    vector<real> a3 = [3.1];
    vector<real> a4 = [6.4];
    var vector<real> z;
    vector<bool> z0 = a2 < a1 ;
    vector<bool> z1 = a2 > a1;
    vector<bool> z2 = a2 >= a1;
    vector<bool> z3 = a2 <= a1;
    vector<bool> z4 = a2 == a1;
    vector<bool> z5 = a2 != a1;
    vector<bool> z6 = a2 == 3 ;
    vector<bool> z7 = a4 < a3;
    vector<bool> z8 = a4 > a3;
    vector<bool> z9 = a4 >= a3;
    vector<bool> z10 = a4 <= a3;
    vector<bool> z11 = a4 == a3;
    vector<bool> z12 = a4 != a3;
    vector<bool> z13 = a4 == 6.4;
    z = a1 + a2 ;
    z = a1 - a2 ;
    z = a1 * a2 ;
    z = a2 / a1 ;
    z = a2 % a1 ;
    z = a2 ^ a2 ;
    z = a3 + a4;
    z = a3 - a4;
    z = a3 * a4;
    z = a4 / a3;
    z = a4 % a3;
    z = a4 ^ a4;

    return 0;
}
//CHECK: