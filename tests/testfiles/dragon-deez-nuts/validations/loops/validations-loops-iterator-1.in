procedure main() returns integer{
    var integer[*][*] v = [i in 1..3,j in 1..3 | i*j];
    loop i in v {
        i -> std_output;
        '\n' -> std_output;
    }
    return 0;
}

//CHECK:TypeError on Line 3: Domain expression must be a 1-D array or vecto
