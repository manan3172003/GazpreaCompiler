/*
This test tries to make a nested struct of tuple
*/

procedure main() returns integer {
    struct Another (character char, real float, tuple (integer, real) struct_field);
    var Another t2;
    return 0;
}

// CHECK:DefinitionError on Line 6: Invalid struct declaration∑