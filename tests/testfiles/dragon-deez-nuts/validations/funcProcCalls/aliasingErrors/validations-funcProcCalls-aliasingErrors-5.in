/*
Procedure call should throw aliasing error
*/

procedure swap(var integer a, var tuple(integer, integer) b) {}

procedure main() returns integer {
  var integer q = 4;
  var tuple(integer, integer) t = (q, 5);
  call swap(t.1, t);   // both arguments alias same lvalue
  return 0;
}

//CHECK:AliasingError on Line 10: