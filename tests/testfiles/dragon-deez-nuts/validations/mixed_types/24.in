/*
This test validates type error: and operator between char and bool.
Tests: const mix_c_b_and = mc and true; should fail
*/
typealias integer int;
typealias boolean bool;
typealias real float;
typealias character char;

procedure main() returns integer {
    char mc = 'z';

    const mix_c_b_and = mc and true; // should TypeError

    return 0;
}

//CHECK:TypeError on line 13
