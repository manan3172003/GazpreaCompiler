/*
This test validates type error: addition between real and bool.
Tests: const mix_r_b_add = mr + mb; should fail
*/
typealias integer int;
typealias boolean bool;
typealias real float;
typealias character char;

procedure main() returns integer {
    float mr = 2.5;
    bool mb = true;

    const mix_r_b_add = mr + mb; // should TypeError

    return 0;
}

//CHECK:TypeError on line 14
