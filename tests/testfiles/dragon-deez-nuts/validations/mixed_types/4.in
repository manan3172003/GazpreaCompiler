/*
This test validates type error: and operator between int and bool.
Tests: const mix_i_b_and = mi and mb; should fail
*/
typealias integer int;
typealias boolean bool;
typealias real float;
typealias character char;

procedure main() returns integer {
    int mi = 7;
    bool mb = true;

    const mix_i_b_and = mi and mb; // should TypeError

    return 0;
}

//CHECK:TypeError on line 14
