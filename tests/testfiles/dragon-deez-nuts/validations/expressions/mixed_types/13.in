/*
This test validates type error: addition between real and char.
Tests: const mix_r_c_add = mr + mc; should fail
*/
typealias integer int;
typealias boolean bool;
typealias real float;
typealias character char;

procedure main() returns integer {
    float mr = 2.5;
    char mc = 'z';

    const mix_r_c_add = mr + mc; // should TypeError

    return 0;
}

//CHECK:TypeError on line 14
