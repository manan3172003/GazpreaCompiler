/*
This test validates type error: and operator between bool and tuple.
Tests: const mix_b_t_and = mb and mt; should fail
*/
typealias integer int;
typealias boolean bool;
typealias real float;
typealias character char;

procedure main() returns integer {
    bool mb = true;
    tuple(int,int) mt = (1, 9);

    const mix_b_t_and = mb and mt; // should TypeError

    return 0;
}

//CHECK:TypeError on line 14
