/*
This test tries to create a struct literal with invalid parameters
*/
typealias struct S(integer x, integer y) Pair;

procedure main() returns integer {
    var p = S(1, 2.0);

    return 0;
}

// CHECK:TypeError on Line 7: Incorrect struct member list