/*
This test validates type error: not operator on tuples.
Tests: const t_not = not t1; should fail
*/
typealias integer int;
typealias boolean bool;
typealias real float;
typealias character char;

procedure main() returns integer {
    tuple(int,int) t1 = (1,2);

    const t_not = not t1;     // should TypeError

    return 0;
}

//CHECK:TypeError on line 13
