/*
This test tries to define a function with same name but different parameter types
*/

procedure myProc(integer x);

procedure main() returns integer{
    call myProc();

    return 0;
}

procedure myProc(real x) {}

//CHECK:DefinitionError on Line 13: Parameter type does not match