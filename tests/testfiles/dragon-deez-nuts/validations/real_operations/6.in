/*
This test validates type error: not operator on floats.
Tests: const r_not = not r1; should fail
*/
typealias integer int;
typealias boolean bool;
typealias real float;
typealias character char;

procedure main() returns integer {
    float r1 = 10.0;

    const r_not = not r1;    // should TypeError

    return 0;
}

//CHECK:TypeError on line 13
